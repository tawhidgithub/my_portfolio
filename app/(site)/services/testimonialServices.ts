import api from "@/lib/api";
import { ENDPOINTS } from "@/lib/endpoient";
import { Testimonial } from "@/app/type/sectionTypes";
import { ApiResponseType } from "@/app/type/responseType";

// export const testimonialService = {
//   getAll: async (): Promise<ApiResponseType<Testimonial[]>> => {
//     const res = await api.get<ApiResponseType<Testimonial[]>>(ENDPOINTS.skills);
//     return res.data;
//   },
// };
